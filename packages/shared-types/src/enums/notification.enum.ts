// Notification enums
export enum NotificationType {
    INFO = 'info',
    SUCCESS = 'success',
    WARNING = 'warning',
    CRITICAL = 'critical',
}

export enum NotificationTargetType {
    INDIVIDUAL = 'individual',
    GROUP = 'group',
    BROADCAST = 'broadcast',
}
