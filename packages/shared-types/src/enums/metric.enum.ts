// Health metric & risk enums
export enum MetricType {
    BLOOD_PRESSURE = 'blood_pressure',
    HEART_RATE = 'heart_rate',
    BMI = 'bmi',
    WEIGHT = 'weight',
    HEIGHT = 'height',
    WATER_INTAKE = 'water_intake',
    KCAL_INTAKE = 'kcal_intake',
}

export enum RiskLevel {
    NORMAL = 'normal',
    WARNING = 'warning',
    DANGER = 'danger',
}
